{
    "schema": 1,
    "type": "completion",
    "description": "From two list of facts compare them and make a score",
    "execution_settings": {
        "default": {
            "max_tokens": 800,
            "temperature": 0
        }
    },
    "input_variables": [
        {
            "name": "rightFacts",
            "description": "The facts that are correct",
            "required": true
        },
        {
            "name": "answer",
            "description": "The student's answer",
            "required": true
        }        
    ]
}